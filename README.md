# VectorShift Frontend Assessment

Run the frontend and FastAPI backend together:

```bash
chmod +x start.sh
./start.sh
```

The launcher creates `.venv` when needed, installs the backend requirements, starts FastAPI on port `8000`, and starts React on port `3000`.
